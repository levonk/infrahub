# Nexus Bundled Assets

Place static files that must be copied into the Nexus image (for example, branding assets or bootstrap configuration archives) inside this directory. Avoid storing secrets or large binaries; reference them through secure storage if required.
