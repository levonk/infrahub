# Verdaccio Runtime Mounts

Stage configuration files, credentials, or other artifacts that Docker mounts into the Verdaccio container here. Maintain clear subfolders per mount path if you add multiple items.
