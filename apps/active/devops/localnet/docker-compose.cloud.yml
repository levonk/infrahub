---
services:
  frps: # https://github.com/fatedier/frp
    image: fatedier/frps:v0.65.0
    container_name: localnet-proxy-frps
    logging: *default-logging
    restart: always
    volumes:
      - ./frps/mounts/etc/frps:/etc/frps
    network_mode: host
    labels:
      - "com.centurylinklabs.watchtower.enable=false"
    command: "-c /etc/frps/frps.toml"

## Maybe add other lightweight filtering services in the cloud too
## traefik, crowdsec, GeoBlock, http->https

## TODO: mail gateway to allow my own outbound email

## TODO: backup DNS to have a cloud sync'd DNS I can connect to

## TODO: VPN to use when outside to go outside or inside

## TODO: Tor to use when going outside not Tor from home
