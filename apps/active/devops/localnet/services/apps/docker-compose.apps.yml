---
# Apps Services Docker Compose Configuration
# Includes all application services that run on top of the base infrastructure

include:
  - ./stfu/docker-compose.stfu.yml
  - ./nuxt-tweet-org/docker-compose.nuxt-tweet-org.yml

# Note: Add more app services here as they are created
# Example:
# - ./my-app/docker-compose.my-app.yml
