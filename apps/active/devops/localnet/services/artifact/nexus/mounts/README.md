# Nexus Runtime Mounts

This directory is the staging area for files that Docker mounts into the running Nexus container. Examples include `nexus.properties`, TLS certificates, or repository configuration exports. Keep the tree organized by mount point and document any sensitive material stored elsewhere.
