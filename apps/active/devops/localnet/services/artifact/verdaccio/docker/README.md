# Verdaccio Docker Context

Store Dockerfiles and build helpers that customize the Verdaccio image. If the stock image requires no changes, leave this directory empty but keep the README to document the intent.
