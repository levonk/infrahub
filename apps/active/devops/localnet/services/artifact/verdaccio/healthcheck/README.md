# Verdaccio Health Checks

Keep liveness/readiness probe scripts, smoke tests, or curl-based checks in this directory. Document any required environment variables inside each script so automation jobs know how to execute them.
