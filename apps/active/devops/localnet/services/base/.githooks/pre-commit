#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "${SCRIPT_DIR}"

if [[ ! -x scripts/run-quality-checks.sh ]]; then
  echo "error: scripts/run-quality-checks.sh is not executable" >&2
  exit 1
fi

scripts/run-quality-checks.sh
