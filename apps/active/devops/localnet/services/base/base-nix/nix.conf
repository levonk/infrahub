# Update this to have the rules from apps/active/devops/localnet/internal-docs/requirements/nix/nix-package-store.md
extra-substituters = https://cache.nixos.org
extra-trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY=
experimental-features = nix-command flakes
trusted-users = root cuser

# sidecar should have a scheduled job (supercronic) run this daily or less often rather than each build
auto-optimise-store = false
