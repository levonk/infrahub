{
  "proxy": {
    "listen": {
      "host": "0.0.0.0",
      "port": 8085
    },
    "target": {
      "host": "claude-code-mcp",
      "port": 8082,
      "protocol": "http"
    },
    "security": {
      "require_auth": false,
      "cors": {
        "enabled": true,
        "allowed_origins": ["*"],
        "allowed_methods": ["GET", "POST", "PUT", "DELETE", "OPTIONS"],
        "allowed_headers": ["*"]
      }
    },
    "timeout": 30000,
    "retry": {
      "max_attempts": 3,
      "backoff_ms": 1000
    }
  },
  "logging": {
    "level": "info",
    "format": "json"
  }
}
