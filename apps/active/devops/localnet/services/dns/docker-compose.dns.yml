# /home/micro/p/gh/lrepo52/job-aide-wt01/apps/active/devops/localnet/services/dns/docker-compose.dns.yml
---

x-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "5"
    tag: "{{.Name}}"
    labels: "service"

include:
  - ../../docker-compose.include.yml

# DNS service stack for Local Network Infrastructure
# The 'localnet' network is defined in the parent docker-compose.yml with proper subnet configuration
services:
  # First peer (not transparent) layer of DNS Services. Handles Blocklists.
  dnsdist:
    build:
      context: ./dnsdist
      dockerfile: docker/Dockerfile.dnsdist
    image: localnet-dns-dnsdist:latest
    container_name: localnet-dns-dnsdist
    logging: *default-logging
    networks:
      - localnet
    ports:
      # Transparent mode: DNSDist listens on container port, forwarded to host port
      # WSL2 limitation: port 53 cannot be bound from containers (Windows reserves it)
      # Using port 5500 - completely clear of conflicts
      - "${DNS_TRANSPARENT_HOST_PORT:-5500}:${DNS_TRANSPARENT_CONTAINER_PORT:-5500}/udp" # Transparent mode UDP
      - "${DNS_TRANSPARENT_HOST_PORT:-5500}:${DNS_TRANSPARENT_CONTAINER_PORT:-5500}/tcp" # Transparent mode TCP
      # Direct mode: DNSDist's secondary listener on container port, forwarded to host port
      # Used for testing/debugging the DNSDist service directly
      - "${DNS_DNSDIST_MAIN_HOST_PORT:-5501}:${DNS_DNSDIST_MAIN_CONTAINER_PORT:-5501}/udp" # Direct mode UDP
      - "${DNS_DNSDIST_MAIN_HOST_PORT:-5501}:${DNS_DNSDIST_MAIN_CONTAINER_PORT:-5501}/tcp" # Direct mode TCP
      # Metrics endpoint for monitoring DNSDist performance
      - "${DNS_DNSDIST_METRICS_HOST_PORT:-8083}:${DNS_DNSDIST_METRICS_CONTAINER_PORT:-8083}/tcp" # Metrics
    volumes:
      - ./dnsdist/mounts/templates:/templates:ro
      - dns-blocklist-cdbs:/etc/dnsdist/blocklists:ro
    env_file:
      - ../../.env
    depends_on:
      coredns:
        condition: service_healthy
      blocklist-compiler:
        condition: service_completed_successfully
    healthcheck:
      test:
        [
          "CMD",
          "dig",
          "+short",
          "example.com",
          "@127.0.0.1",
          "-p",
          "${DNS_DNSDIST_MAIN_CONTAINER_PORT:-5353}",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 170s
    restart: unless-stopped
    profiles:
      - todo
      - all
  # AdGuard Home: DNS-based ad blocker and network-wide tracking protection
  # Role: Provides DNS filtering, ad blocking, and DNS query logging
  # Can be used as an alternative to blocklists or as an additional filtering layer
  adguard:
    build:
      context: ./adguard
      dockerfile: ./Dockerfile.adguard
    image: localnet-dns-adguard:latest
    container_name: localnet-dns-adguard
    logging: *default-logging
    networks:
      localnet:
        ipv4_address: ${DNS_ADGUARD_IP:-172.26.255.67}
        aliases:
          - adguard
    ports:
      # AdGuard Home DNS service (avoiding port 53 due to WSL2 limitations)
      - "${DNS_ADGUARD_HOST_PORT:-5354}:${DNS_ADGUARD_CONTAINER_PORT:-5354}/udp" # DNS UDP
      - "${DNS_ADGUARD_HOST_PORT:-5354}:${DNS_ADGUARD_CONTAINER_PORT:-5354}/tcp" # DNS TCP
      # AdGuard Home web interface
      - "${DNS_ADGUARD_WEB_HOST_PORT:-3000}:3000/tcp" # Web UI
    volumes:
      - dns-adguard-conf:/opt/adguardhome/conf
      - dns-adguard-work:/opt/adguardhome/work
    env_file:
      - ../../.env
    environment:
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - TZ=${TZ:-UTC}
      - DNS_ADGUARD_IP=${DNS_ADGUARD_IP:-172.26.255.67}
      - DNS_ADGUARD_HOST_PORT=${DNS_ADGUARD_HOST_PORT:-5354}
      - DNS_ADGUARD_CONTAINER_PORT=${DNS_ADGUARD_CONTAINER_PORT:-5354}
      - DNS_ADGUARD_WEB_HOST_PORT=${DNS_ADGUARD_WEB_HOST_PORT:-3000}
      - DNS_ADGUARD_WEB_CONTAINER_PORT=${DNS_ADGUARD_WEB_CONTAINER_PORT:-3000}
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    profiles:
      - "broken"
      - all

  # second layer of DNS Services. Handles fallback, and normal caching
  coredns:
    # CoreDNS: Primary DNS resolver with caching and DNSSEC validation
    # Role: Serves as the primary upstream resolver for DNSDist
    # Provides recursive DNS resolution, local zone support, and DNSSEC validation
    # Falls back to dnscrypt-proxy if CoreDNS becomes unavailable (via DNSDist fallback rules)
    build:
      context: ./coredns
      dockerfile: docker/Dockerfile.coredns
    image: localnet-dns-coredns:latest
    container_name: localnet-dns-coredns
    logging: *default-logging
    networks:
      localnet:
        ipv4_address: ${DNS_COREDNS_MAIN_IP:-172.26.255.51}
        aliases:
          - coredns
    ports:
      # Direct mode: CoreDNS listens on container :${DNS_COREDNS_MAIN_CONTAINER_PORT}, forwarded to host
      # Primary DNS resolver with caching and DNSSEC validation
      # DNSDist forwards queries to this service as the primary upstream
      - "${DNS_COREDNS_MAIN_HOST_PORT:-15354}:${DNS_COREDNS_MAIN_CONTAINER_PORT:-15353}/udp" # Direct mode UDP
      - "${DNS_COREDNS_MAIN_HOST_PORT:-15354}:${DNS_COREDNS_MAIN_CONTAINER_PORT:-15353}/tcp" # Direct mode TCP
      # Metrics endpoint for monitoring CoreDNS performance
      - "${DNS_COREDNS_MAIN_METRICS_HOST_PORT:-9153}:9153/tcp" # Metrics
      # Health check endpoint (default host port: 27493)
      - "${DNS_COREDNS_MAIN_HEALTH_HOST_PORT:-27493}:${DNS_COREDNS_MAIN_HEALTH_CONTAINER_PORT:-18080}/tcp" # Health
    environment:
      - DNS_COREDNS_MAIN_IP=${DNS_COREDNS_MAIN_IP:-172.26.255.51}
      - DNS_COREDNS_MAIN_HOST_PORT=${DNS_COREDNS_MAIN_HOST_PORT:-15354}
      - DNS_COREDNS_MAIN_CONTAINER_PORT=${DNS_COREDNS_MAIN_CONTAINER_PORT:-15353}
      - DNS_COREDNS_MAIN_METRICS_HOST_PORT=${DNS_COREDNS_MAIN_METRICS_HOST_PORT:-9153}
      - DNS_COREDNS_MAIN_HEALTH_HOST_PORT=${DNS_COREDNS_MAIN_HEALTH_HOST_PORT:-27493}
      - DNS_COREDNS_MAIN_HEALTH_CONTAINER_PORT=${DNS_COREDNS_MAIN_HEALTH_CONTAINER_PORT:-18080}
      - DNS_DNSCRYPT_ODOH_IP=${DNS_DNSCRYPT_ODOH_IP:-172.26.255.60}
      - DNS_DNSCRYPT_ANON_IP=${DNS_DNSCRYPT_ANON_IP:-172.26.255.61}
      - DNS_DNSCRYPT_STD_IP=${DNS_DNSCRYPT_STD_IP:-172.26.255.62}
      - DNS_DNSCRYPT_DOH_IP=${DNS_DNSCRYPT_DOH_IP:-172.26.255.63}
      - DNS_DNSCRYPT_ENCRYPTED_IP=${DNS_DNSCRYPT_ENCRYPTED_IP:-172.26.255.64}
      - DNS_DNSCRYPT_PLAINTEXT_IP=${DNS_DNSCRYPT_PLAINTEXT_IP:-172.26.255.65}
      - DNS_DNSCRYPT_TOR_IP=${DNS_DNSCRYPT_TOR_IP:-172.26.255.66}
      - DNS_DNSCRYPT_TOR_CONTAINER_PORT=${DNS_DNSCRYPT_TOR_CONTAINER_PORT:-5058}
      - DNS_DNSCRYPT_TOR_HOST_PORT=${DNS_DNSCRYPT_TOR_HOST_PORT:-5058}
      - DNS_DNSCRYPT_ODOH_CONTAINER_PORT=${DNS_DNSCRYPT_ODOH_CONTAINER_PORT:-5058}
      - DNS_DNSCRYPT_ANON_CONTAINER_PORT=${DNS_DNSCRYPT_ANON_CONTAINER_PORT:-5058}
      - DNS_DNSCRYPT_STD_CONTAINER_PORT=${DNS_DNSCRYPT_STD_CONTAINER_PORT:-5058}
      - DNS_DNSCRYPT_DOH_CONTAINER_PORT=${DNS_DNSCRYPT_DOH_CONTAINER_PORT:-5058}
      - DNS_DNSCRYPT_ENCRYPTED_CONTAINER_PORT=${DNS_DNSCRYPT_ENCRYPTED_CONTAINER_PORT:-5058}
      - DNS_DNSCRYPT_PLAINTEXT_CONTAINER_PORT=${DNS_DNSCRYPT_PLAINTEXT_CONTAINER_PORT:-5058}
    volumes:
      - ./coredns/mounts/templates:/templates:ro
      - dns-coredns-cache:/var/lib/coredns:rw
    depends_on:
      dnscrypt-plaintext:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "curl",
          "-f",
          "http://127.0.0.1:${DNS_COREDNS_MAIN_HEALTH_CONTAINER_PORT:-18080}/health",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    profiles:
      - todo
      - all

  # third layer of DNS Services. Handles DNS native outbound communication
  #  dnscrypt-odoh:
  #    build:
  #      context: ./dnscrypt
  #      dockerfile: docker/Dockerfile.dnscrypt-proxy
  #    image: localnet-dns-dnscrypt-odoh:latest
  #    container_name: localnet-dns-dnscrypt-odoh
  #    networks:
  #      - localnet
  #    volumes:
  #      - ./dnscrypt/mounts/templates:/templates:ro
  #    ports:
  #      - "${DNS_DNSCRYPT_ODOH_HOST_PORT:-5360}:${DNS_DNSCRYPT_ODOH_CONTAINER_PORT:-5053}/udp"
  #      - "${DNS_DNSCRYPT_ODOH_HOST_PORT:-5360}:${DNS_DNSCRYPT_ODOH_CONTAINER_PORT:-5053}/tcp"
  #    environment:
  #      - DNS_DNSCRYPT_ODOH_CONTAINER_PORT=${DNS_DNSCRYPT_ODOH_CONTAINER_PORT:-5053}
  #      - PROXY_TOR_SOCKS5_IP=${PROXY_TOR_SOCKS5_IP:-172.20.255.70}
  #      - PROXY_TOR_SOCKS5_CONTAINER_PORT=${PROXY_TOR_SOCKS5_CONTAINER_PORT:-9050}
  #    # IMPORTANT: Pass ONLY the config name WITHOUT .toml extension to the entrypoint script.
  #    # The script prepends /etc/dnscrypt-proxy/ and appends .toml.template automatically.
  #    # Each service must specify its own config variant (odoh, anon, std, doh, encrypted, plaintext, tor)
  #    command: ["dnscrypt-proxy-odoh"]
  #    restart: unless-stopped
  #
  #  dnscrypt-anon:
  #    build:
  #      context: ./dnscrypt
  #      dockerfile: docker/Dockerfile.dnscrypt-proxy
  #    image: localnet-dns-dnscrypt-anon:latest
  #    container_name: localnet-dns-dnscrypt-anon
  #    networks:
  #      - localnet
  #    ports:
  #      - "${DNS_DNSCRYPT_ANON_HOST_PORT:-5361}:${DNS_DNSCRYPT_ANON_CONTAINER_PORT:-5054}/udp"
  #      - "${DNS_DNSCRYPT_ANON_HOST_PORT:-5361}:${DNS_DNSCRYPT_ANON_CONTAINER_PORT:-5054}/tcp"
  #    environment:
  #      - DNS_DNSCRYPT_ANON_CONTAINER_PORT=${DNS_DNSCRYPT_ANON_CONTAINER_PORT:-5054}
  #      - PROXY_TOR_SOCKS5_IP=${PROXY_TOR_SOCKS5_IP:-172.20.255.70}
  #      - PROXY_TOR_SOCKS5_CONTAINER_PORT=${PROXY_TOR_SOCKS5_CONTAINER_PORT:-9050}
  #    command: ["dnscrypt-proxy-anon"]
  #    restart: unless-stopped
  #
  # third layer of DNS Services. Handles DNS native outbound communication
  #  dnscrypt-proxy:
  #    build:
  #      context: ./dnscrypt
  #      dockerfile: docker/Dockerfile.dnscrypt-proxy
  #    image: localnet-dns-dnscrypt-proxy:latest
  #    container_name: localnet-dns-dnscrypt-proxy
  #    networks:
  #      - localnet
  #    ports:
  #      - "${DNS_DNSCRYPT_STD_HOST_PORT:-5362}:${DNS_DNSCRYPT_STD_CONTAINER_PORT:-5055}/udp"
  #      - "${DNS_DNSCRYPT_STD_HOST_PORT:-5362}:${DNS_DNSCRYPT_STD_CONTAINER_PORT:-5055}/tcp"
  #    environment:
  #      - DNS_DNSCRYPT_STD_CONTAINER_PORT=${DNS_DNSCRYPT_STD_CONTAINER_PORT:-5055}
  #      - PROXY_TOR_SOCKS5_IP=${PROXY_TOR_SOCKS5_IP:-172.20.255.70}
  #      - PROXY_TOR_SOCKS5_CONTAINER_PORT=${PROXY_TOR_SOCKS5_CONTAINER_PORT:-9050}
  #    command: ["dnscrypt-proxy-std"]
  #    restart: unless-stopped
  #
  # third layer of DNS Services. Handles DNS native outbound communication
  #  dnscrypt-doh:
  #    build:
  #      context: ./dnscrypt
  #      dockerfile: docker/Dockerfile.dnscrypt-proxy
  #    image: localnet-dns-dnscrypt-doh:latest
  #    container_name: localnet-dns-dnscrypt-doh
  #    networks:
  #      - localnet
  #    ports:
  #      - "${DNS_DNSCRYPT_DOH_HOST_PORT:-5363}:${DNS_DNSCRYPT_DOH_CONTAINER_PORT:-5056}/udp"
  #      - "${DNS_DNSCRYPT_DOH_HOST_PORT:-5363}:${DNS_DNSCRYPT_DOH_CONTAINER_PORT:-5056}/tcp"
  #    environment:
  #      - DNS_DNSCRYPT_DOH_CONTAINER_PORT=${DNS_DNSCRYPT_DOH_CONTAINER_PORT:-5056}
  #      - PROXY_TOR_SOCKS5_IP=${PROXY_TOR_SOCKS5_IP:-172.20.255.70}
  #      - PROXY_TOR_SOCKS5_CONTAINER_PORT=${PROXY_TOR_SOCKS5_CONTAINER_PORT:-9050}
  #    command: ["dnscrypt-proxy-doh"]
  #    restart: unless-stopped
  #
  # third layer of DNS Services. Handles DNS native outbound communication
  #  dnscrypt-encrypted:
  #    build:
  #      context: ./dnscrypt
  #      dockerfile: docker/Dockerfile.dnscrypt-proxy
  #    image: localnet-dns-dnscrypt-encrypted:latest
  #    container_name: localnet-dns-dnscrypt-encrypted
  #    networks:
  #      - localnet
  #    ports:
  #      - "${DNS_DNSCRYPT_ENCRYPTED_HOST_PORT:-5364}:${DNS_DNSCRYPT_ENCRYPTED_CONTAINER_PORT:-5057}/udp"
  #      - "${DNS_DNSCRYPT_ENCRYPTED_HOST_PORT:-5364}:${DNS_DNSCRYPT_ENCRYPTED_CONTAINER_PORT:-5057}/tcp"
  #    environment:
  #      - DNS_DNSCRYPT_ENCRYPTED_CONTAINER_PORT=${DNS_DNSCRYPT_ENCRYPTED_CONTAINER_PORT:-5057}
  #      - PROXY_TOR_SOCKS5_IP=${PROXY_TOR_SOCKS5_IP:-172.20.255.70}
  #      - PROXY_TOR_SOCKS5_CONTAINER_PORT=${PROXY_TOR_SOCKS5_CONTAINER_PORT:-9050}
  #    command: ["dnscrypt-proxy-encrypted"]
  #    restart: unless-stopped

  # third layer of DNS Services. Handles DNS native outbound communication
  dnscrypt-plaintext:
    build:
      context: ./dnscrypt
      dockerfile: docker/Dockerfile.dnscrypt-proxy
    image: localnet-dns-dnscrypt-plaintext:latest
    container_name: localnet-dns-dnscrypt-plaintext
    logging: *default-logging
    networks:
      localnet:
        ipv4_address: ${DNS_DNSCRYPT_PLAINTEXT_CONTAINER_IP:-172.26.255.65}
    volumes:
      - ./dnscrypt/mounts/templates:/templates:ro
    ports:
      - "${DNS_DNSCRYPT_PLAINTEXT_HOST_PORT:-5365}:${DNS_DNSCRYPT_PLAINTEXT_CONTAINER_PORT:-5058}/udp"
      - "${DNS_DNSCRYPT_PLAINTEXT_HOST_PORT:-5365}:${DNS_DNSCRYPT_PLAINTEXT_CONTAINER_PORT:-5058}/tcp"
    environment:
      - DNS_DNSCRYPT_PLAINTEXT_CONTAINER_PORT=${DNS_DNSCRYPT_PLAINTEXT_CONTAINER_PORT:-5058}
    command:
      ["/usr/local/bin/entrypoint-dnscrypt.sh", "dnscrypt-proxy-plaintext"]
    restart: unless-stopped
    profiles:
      - todo
      - all

  # third layer of DNS Services. Handles DNS native outbound communication
  #  dnscrypt-tor:
  #    build:
  #      context: ./dnscrypt
  #      dockerfile: docker/Dockerfile.dnscrypt-proxy
  #    image: localnet-dns-dnscrypt-tor:latest
  #    container_name: localnet-dns-dnscrypt-tor
  #    networks:
  #      - localnet
  #    ports:
  #      - "${DNS_DNSCRYPT_TOR_HOST_PORT:-5366}:${DNS_DNSCRYPT_TOR_CONTAINER_PORT:-5066}/udp"
  #      - "${DNS_DNSCRYPT_TOR_HOST_PORT:-5366}:${DNS_DNSCRYPT_TOR_CONTAINER_PORT:-5066}/tcp"
  #    environment:
  #      - DNS_DNSCRYPT_TOR_CONTAINER_PORT=${DNS_DNSCRYPT_TOR_CONTAINER_PORT:-5066}
  #      - PROXY_TOR_SOCKS5_IP=${PROXY_TOR_SOCKS5_IP:-172.20.255.70}
  #      - PROXY_TOR_SOCKS5_CONTAINER_PORT=${PROXY_TOR_SOCKS5_CONTAINER_PORT:-9050}
  #    command: ["dnscrypt-proxy-tor"]
  #    depends_on:
  #      tor-proxy:
  #        condition: service_healthy
  #    restart: unless-stopped

  # Foundational Tor proxy services, used by other network services
  tor-proxy:
    build:
      context: ../proxy/tor
      dockerfile: docker/Dockerfile.tor
    image: localnet-proxy-tor:latest
    container_name: localnet-proxy-tor
    logging: *default-logging
    networks:
      localnet:
        ipv4_address: 172.26.255.70
    ports:
      - "${PROXY_TOR_SOCKS5_HOST_PORT:-9050}:${PROXY_TOR_SOCKS5_CONTAINER_PORT:-9050}/tcp"
    environment:
      - PROXY_TOR_SOCKS5_CONTAINER_PORT=${PROXY_TOR_SOCKS5_CONTAINER_PORT:-9050}
    restart: unless-stopped
    profiles:
      - todo
      - all

  # Job container that creates DNSDIST cdb based blocklists
  blocklist-compiler:
    build:
      context: ./dns-blocklists
      dockerfile: Dockerfile.blocklist-compiler
      args:
        PUID: ${PUID:-1000}
        PGID: ${PGID:-1000}
    container_name: localnet-dns-blocklist-compiler
    logging: *default-logging
    networks:
      - localnet
    environment:
      - TZ=UTC
      - PUID=1000
      - PGID=1000
    volumes:
      - dns-blocklist-archive:/blocklists/sources:rw
      - dns-blocklist-cdbs:/blocklists/compiled:rw
    #read_only: true
    tmpfs:
      - /tmp:mode=1777,size=512m
    healthcheck:
      test: ["CMD", "test", "-f", "/blocklists/compiled/blocklist.cdb"]
      interval: 30s
      timeout: 10s
      retries: 3
    profiles:
      - todo
      - all
    restart: "no" # Run once on startup, use n8n/cron/systemd for updates
