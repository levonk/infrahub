---
name: default-ip-remediation
debug: true
filters:
  - Alert.Remediation == true && Alert.GetScope() == "Ip"
decisions:
  - type: ban
    duration: 672h
#duration_expr: Sprintf('%dh', (GetDecisionsCount(Alert.GetValue()) + 1) * 4)
#  - http_default # Set the required http parameters in /etc/crowdsec/notifications/http.yaml before enabling this.
#  - slack_default # Set the required http parameters in /etc/crowdsec/notifications/slack.yaml before enabling this.
#  - splunk_default # Set the required http parameters in /etc/crowdsec/notifications/splunk.yaml before enabling this.
#  - email_default # Set the required http parameters in /etc/crowdsec/notifications/email.yaml before enabling this.
on_success: break
