# Levonk client-specific service catalog entries
#
# This file contains service catalog entries that are specific to the levonk
# client (hostnames, machine-specific deployments). Generic/shared service
# entries live in shared/active/02-config/ansible/infrastructure/services.yml.
#
# See shared/active/02-config/ansible/infrastructure/README.md for the schema.

# ===========================================================================
# VPN / Exit Nodes
# ===========================================================================

- name: "Host Exit Node (lzkmbp2016)"
  container: "—"
  machine: "lzkmbp2016"
  category: "vpn"
  description: "Host-level Tailscale exit node — plain egress via lzkmbp2016's internet (laptop)"
  source_repo: "https://github.com/tailscale/tailscale"

- name: "Host Exit Node (lzkmbp2018)"
  container: "—"
  machine: "lzkmbp2018"
  category: "vpn"
  description: "Host-level Tailscale exit node — plain egress via lzkmbp2018's internet (laptop)"
  source_repo: "https://github.com/tailscale/tailscale"
