---
# Role metadata for isolation-vm-containers
galaxy_info:
  role_name: isolation_vm_containers
  author: levonk
  description: Deploys containers inside the Isolation VM for AI agent operations
  company: levonk
  license: MIT
  min_ansible_version: "2.9"
  platforms:
    - name: Debian
      versions:
        - bookworm
        - bullseye
  galaxy_tags:
    - containers
    - docker
    - isolation
    - nix
    - ai

dependencies:
  - role: isolation-vm-docker
  - role: isolation-vm-config