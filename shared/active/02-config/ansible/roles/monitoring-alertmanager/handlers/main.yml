---
# handlers file for monitoring-alertmanager

- name: restart monitoring-alertmanager
  community.docker.docker_container:
    name: "{{ monitoring_alertmanager_container_name }}"
    state: started
    restart: true
  listen: restart monitoring-alertmanager
