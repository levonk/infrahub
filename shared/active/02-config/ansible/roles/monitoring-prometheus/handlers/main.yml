---
# handlers file for monitoring-prometheus

- name: restart monitoring-prometheus
  community.docker.docker_container:
    name: "{{ monitoring_prometheus_container_name }}"
    state: started
    restart: true
  listen: restart monitoring-prometheus
