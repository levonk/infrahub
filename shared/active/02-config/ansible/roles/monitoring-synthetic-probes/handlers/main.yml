---
# handlers file for monitoring-synthetic-probes

- name: restart monitoring-synthetic-probes
  community.docker.docker_container:
    name: "{{ monitoring_synthetic_probes_container_name }}"
    state: started
    restart: true
  listen: restart monitoring-synthetic-probes
