---
galaxy_info:
  role_name: nix_ncro
  author: levonk
  description: ncro — Nix parallel racing proxy (behind ncps), stateless, Windows Docker Desktop
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Windows
      versions: ["all"]
  galaxy_tags:
    - nix
    - cache
    - proxy
    - racing
    - docker
dependencies: []
