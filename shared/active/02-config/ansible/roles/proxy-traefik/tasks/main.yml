---
# Traefik Proxy Deployment Tasks
# Deploys Traefik reverse proxy with ACME, plugins, and security middleware
# All ports are variable-driven per AGENTS.md rules.

# ---------------------------------------------------------------------------
# Prerequisites
# ---------------------------------------------------------------------------
- name: Ensure Traefik data directory exists
  ansible.builtin.file:
    path: "{{ proxy_traefik_data_dir }}"
    state: directory
    owner: root
    group: root
    mode: "{{ infra_mode_dir }}"
  become: true

- name: Ensure Traefik configuration directories exist
  ansible.builtin.file:
    path: "{{ proxy_traefik_data_dir }}/{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "{{ infra_mode_dir }}"
  become: true
  loop:
    - config
    - config/static
    - config/dynamic
    - ssl

# ---------------------------------------------------------------------------
# Docker Networks
# ---------------------------------------------------------------------------
- name: Ensure Traefik Docker network exists
  community.docker.docker_network:
    name: "{{ proxy_traefik_network_name }}"
    state: present
    driver: bridge
    ipam_config:
      - subnet: "{{ proxy_traefik_network_subnet }}"
        gateway: "{{ proxy_traefik_network_gateway }}"
  become: true

# ---------------------------------------------------------------------------
# Docker Volumes
# ---------------------------------------------------------------------------
- name: Ensure Traefik SSL volume exists
  community.docker.docker_volume:
    name: "{{ proxy_traefik_ssl_volume }}"
    state: present
  become: true

- name: Ensure Traefik config volume exists
  community.docker.docker_volume:
    name: "{{ proxy_traefik_config_volume }}"
    state: present
  become: true

# ---------------------------------------------------------------------------
# Deploy Traefik Static Configuration
# ---------------------------------------------------------------------------
- name: Deploy traefik.yml static configuration
  ansible.builtin.template:
    src: traefik.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/traefik.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: restart traefik

# ---------------------------------------------------------------------------
# Deploy Traefik Dynamic Configuration Templates
# ---------------------------------------------------------------------------
- name: Ensure dynamic configuration directory exists
  ansible.builtin.file:
    path: "{{ proxy_traefik_data_dir }}/config/dynamic"
    state: directory
    owner: root
    group: root
    mode: "{{ infra_mode_dir }}"
  become: true

- name: Deploy middleware configuration template
  ansible.builtin.template:
    src: dynamic/middlewares.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/middlewares.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik

- name: Deploy SearXNG dynamic configuration template
  ansible.builtin.template:
    src: dynamic/search.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/search.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: search_searxng_enabled | bool

- name: Deploy Authelia dynamic configuration template
  ansible.builtin.template:
    src: dynamic/auth.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/auth.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: proxy_authelia_enabled | bool

- name: Deploy agentmemory dynamic configuration template
  ansible.builtin.template:
    src: dynamic/agentmemory.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/agentmemory.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: agentmemory_enabled | default(false) | bool

- name: Deploy paperclip dynamic configuration template
  ansible.builtin.template:
    src: dynamic/paperclip.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/paperclip.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: ai_paperclip_enabled | default(false) | bool

- name: Deploy aishrink (headroom) dynamic configuration template
  ansible.builtin.template:
    src: dynamic/aishrink.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/aishrink.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: aishrink_enabled | default(false) | bool

- name: Deploy Traefik API dynamic configuration template
  ansible.builtin.template:
    src: dynamic/traefik-api.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/traefik-api.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: proxy_traefik_api_entrypoint_enabled | default(false) | bool

- name: Deploy Homepage (start) dynamic configuration template
  ansible.builtin.template:
    src: dynamic/start.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/start.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: dashboard_homepage_enabled | default(true) | bool

- name: Deploy TraLa (start2) dynamic configuration template
  ansible.builtin.template:
    src: dynamic/start2.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/start2.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: dashboard_trala_enabled | default(true) | bool

- name: Deploy Homepage (start.cno) dynamic configuration template
  ansible.builtin.template:
    src: dynamic/start-cno.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/start-cno.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: dashboard_homepage_enabled | default(true) | bool

- name: Deploy TraLa (start2.cno) dynamic configuration template
  ansible.builtin.template:
    src: dynamic/start2-cno.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/start2-cno.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: dashboard_trala_enabled | default(true) | bool

- name: Deploy OmniRoute (airoute) dynamic configuration template
  ansible.builtin.template:
    src: dynamic/airoute.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/airoute.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: ai_omniroute_enabled | default(false) | bool

- name: Deploy LiteLLM (aigate) dynamic configuration template
  ansible.builtin.template:
    src: dynamic/aigate.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/aigate.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: ai_litellm_enabled | default(false) | bool

- name: Deploy Buzz dynamic configuration template
  ansible.builtin.template:
    src: dynamic/buzz.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/buzz.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: ai_buzz_enabled | default(false) | bool

- name: Deploy AI InterFace (aiif / Omnigent) dynamic configuration template
  ansible.builtin.template:
    src: dynamic/aiif.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/aiif.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: aiif_enabled | default(false) | bool

- name: Deploy JobOps dynamic configuration template
  ansible.builtin.template:
    src: dynamic/jobops.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/jobops.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: career_jobops_enabled | default(false) | bool

- name: Deploy RustFS dynamic configuration template
  ansible.builtin.template:
    src: dynamic/rustfs.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/rustfs.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: rustfs_enabled | default(false) | bool

- name: Deploy copyparty dynamic configuration template
  ansible.builtin.template:
    src: dynamic/copyparty.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/copyparty.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: copyparty_enabled | default(false) | bool
  tags: ["deploy", "traefik", "copyparty"]

- name: Deploy Wazuh dynamic configuration template
  ansible.builtin.template:
    src: dynamic/wazuh.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/wazuh.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: wazuh_enabled | default(false) | bool

- name: Deploy Verdaccio (cno) dynamic configuration template
  ansible.builtin.template:
    src: dynamic/npmjs-cno.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/npmjs-cno.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: verdaccio_enabled | default(false) | bool
  tags: ["deploy", "traefik", "verdaccio"]

- name: Deploy Nix Cache Chain (cno) dynamic configuration template
  ansible.builtin.template:
    src: dynamic/nixcache-cno.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/nixcache-cno.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: nix_ncps_enabled | default(false) | bool
  tags: ["deploy", "traefik", "nix-cache"]

- name: Deploy AdGuard Home dynamic configuration template
  ansible.builtin.template:
    src: dynamic/adguard.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/adguard.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: dns_adguard_enabled | default(false) | bool
  tags: ["deploy", "traefik", "adguard"]

- name: Deploy Control Center dashboard (default domain) dynamic configuration template
  ansible.builtin.template:
    src: dynamic/cc-default.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/cc-default.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: control_center_enabled | default(false) | bool
  tags: ["deploy", "traefik", "control-center"]

- name: Deploy Control Center dashboard (NL domain) dynamic configuration template
  ansible.builtin.template:
    src: dynamic/cc-nl.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/cc-nl.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: control_center_enabled | default(false) | bool
  tags: ["deploy", "traefik", "control-center"]

- name: Deploy Langfuse dynamic configuration template
  ansible.builtin.template:
    src: dynamic/langfuse.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/langfuse.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: ai_langfuse_enabled | default(false) | bool
  tags: ["deploy", "traefik", "langfuse"]

- name: Deploy CrowdSec dynamic configuration template
  ansible.builtin.template:
    src: dynamic/crowdsec.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/crowdsec.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: security_crowdsec_enabled | default(false) | bool
  tags: ["deploy", "traefik", "crowdsec"]

- name: Deploy Langfuse MinIO dynamic configuration template
  ansible.builtin.template:
    src: dynamic/langfuse-minio.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/langfuse-minio.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: ai_langfuse_enabled | default(false) | bool
  tags: ["deploy", "traefik", "langfuse-minio"]

# Web Proxy Chain dynamic configs (see diagrams/proxy/complete-web-proxy-chain.mmd)
- name: Deploy MITM proxy web UI dynamic configuration template
  ansible.builtin.template:
    src: dynamic/mitm.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/mitm.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: proxy_web_mitm_enabled | default(false) | bool
  tags: ["deploy", "traefik", "proxy-web", "mitm"]

- name: Deploy Varnish admin dynamic configuration template
  ansible.builtin.template:
    src: dynamic/varnish.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/varnish.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: proxy_web_varnish_enabled | default(false) | bool
  tags: ["deploy", "traefik", "proxy-web", "varnish"]

- name: Deploy MITM CA cert download dynamic configuration template
  ansible.builtin.template:
    src: dynamic/mitm-ca.yml.j2
    dest: "{{ proxy_traefik_data_dir }}/config/dynamic/mitm-ca.yml"
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  notify: reload traefik
  when: proxy_web_mitm_enabled | default(false) | bool
  tags: ["deploy", "traefik", "proxy-web", "mitm-ca"]

# ---------------------------------------------------------------------------
# Traefik Container
# ---------------------------------------------------------------------------
- name: Deploy Traefik container
  community.docker.docker_container:
    name: "{{ proxy_traefik_container_name }}"
    image: "{{ proxy_traefik_image }}:{{ proxy_traefik_image_tag }}"
    state: started
    restart_policy: unless-stopped
    networks:
      - name: "{{ proxy_traefik_network_name }}"
    published_ports:
      - "{{ proxy_traefik_host_ip }}:{{ proxy_traefik_http_port }}:{{ proxy_traefik_http_container_port }}/tcp"
      - "{{ proxy_traefik_host_ip }}:{{ proxy_traefik_https_port }}:{{ proxy_traefik_https_container_port }}/tcp"
      - "{{ infra_network_bind_localhost }}:{{ proxy_traefik_dashboard_port }}:{{ proxy_traefik_dashboard_container_port }}/tcp"
      - "{{ proxy_traefik_host_ip }}:{{ proxy_traefik_metrics_port }}:{{ proxy_traefik_metrics_container_port }}/tcp"
    volumes:
      - "{{ proxy_traefik_ssl_volume }}:/letsencrypt"
      - "{{ proxy_traefik_data_dir }}/config:/etc/traefik:ro"
    env:
      TZ: "{{ proxy_traefik_tz }}"
      CF_DNS_API_TOKEN: "{{ vault_cloudflare_api_token | default('change-me') }}"
    command:
      - "--configFile=/etc/traefik/traefik.yml"
    log_driver: json-file
    log_opt:
      max-size: "{{ proxy_traefik_log_max_size }}"
      max-file: "{{ proxy_traefik_log_max_file }}"
    labels:
      traefik.enable: "false"
    groups:
      - "{{ docker_group_gid | default('0') }}"  # project-lint: disable=magic-int  fallback GID for Docker group
  become: true
  when: proxy_traefik_enabled | bool

# ---------------------------------------------------------------------------
# Log Retention Configuration
# ---------------------------------------------------------------------------
- name: Deploy logrotate configuration for Traefik
  ansible.builtin.template:
    src: logrotate/traefik.j2
    dest: /etc/logrotate.d/traefik
    owner: root
    group: root
    mode: "{{ infra_mode_file }}"
  become: true
  when: proxy_traefik_enabled | bool

# ---------------------------------------------------------------------------
# Health Verification
# ---------------------------------------------------------------------------
- name: Wait for Traefik dashboard to be healthy
  ansible.builtin.uri:
    url: "http://{{ infra_network_bind_localhost }}:{{ proxy_traefik_dashboard_port }}{{ proxy_traefik_dashboard_endpoint }}"
    status_code: [200, 401]
    return_content: false
  register: traefik_health
  until: traefik_health.status in [200, 401]
  retries: "{{ infra_retry_long }}"
  delay: "{{ infra_delay_default }}"
  when:
    - proxy_traefik_enabled | bool
    - proxy_traefik_verify_health | bool

# ---------------------------------------------------------------------------
# Status Report
# ---------------------------------------------------------------------------
- name: Report Traefik status
  ansible.builtin.debug:
    msg: |2
      Traefik deployed successfully:
      - HTTP: http://{{ ansible_host | default('localhost') }}:{{ proxy_traefik_http_port }}
      - HTTPS: https://{{ ansible_host | default('localhost') }}:{{ proxy_traefik_https_port }}
      - Dashboard: http://{{ ansible_host | default('localhost') }}:{{ proxy_traefik_dashboard_port }}{{ proxy_traefik_dashboard_endpoint }}
      - Network: {{ proxy_traefik_network_name }}
      - ACME: {{ proxy_traefik_acme_enabled }}
      - Plugins: {{ proxy_traefik_plugins_enabled }}
  when: proxy_traefik_enabled | bool
