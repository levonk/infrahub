# Traefik Dynamic Configuration - TraLa (start2.cno.levonk.com)
# Generated by Ansible - DO NOT EDIT MANUALLY
# Domain: {{ infra_domain_dashboard_trala_cno | default('start2.cno.levonk.com') }}
# Purpose: cno-specific TraLa dashboard — routes to the same trala container
# Security Chain: Authelia

http:
  routers:
    trala-cno-http:
      rule: "Host(`{{ infra_domain_dashboard_trala_cno | default('start2.cno.levonk.com') }}`)"
      entryPoints:
        - web
      middlewares:
        - redirect-to-https
      service: trala-cno

    trala-cno-https:
      rule: "Host(`{{ infra_domain_dashboard_trala_cno | default('start2.cno.levonk.com') }}`)"
      entryPoints:
        - websecure
      middlewares:
        - authelia
      service: trala-cno
      tls:
        certResolver: letsencrypt

  services:
    trala-cno:
      loadBalancer:
        servers:
          - url: "http://{{ dashboard_trala_container_name | default('trala') }}:{{ dashboard_trala_container_port | default('8080') }}"
        passHostHeader: true
