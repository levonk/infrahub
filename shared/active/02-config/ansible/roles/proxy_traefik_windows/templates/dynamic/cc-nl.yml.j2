# Traefik Dynamic Configuration - Control Center (nl) Routing
# Generated by Ansible - DO NOT EDIT MANUALLY
# Domain: {{ proxy_traefik_windows_control_center_domain_nl }}
#
# Routes to the control-center container (dashboard).
# Web UI is behind Authelia SSO (forwarded to OCI via Tailscale).

http:
  routers:
    # HTTP redirect to HTTPS
    control-center-nl-http:
      rule: "Host(`{{ proxy_traefik_windows_control_center_domain_nl }}`)"
      entryPoints:
        - web
      middlewares:
        - redirect-to-https
      service: control-center-nl

    # HTTPS — Web UI behind Authelia SSO
    control-center-nl-https:
      rule: "Host(`{{ proxy_traefik_windows_control_center_domain_nl }}`)"
      entryPoints:
        - websecure
{% if proxy_traefik_windows_authelia_enabled %}
      middlewares:
        - authelia
{% endif %}
      service: control-center-nl
      tls:
        certResolver: letsencrypt

  services:
    control-center-nl:
      loadBalancer:
        servers:
          - url: "http://{{ proxy_traefik_windows_control_center_container_name }}:{{ proxy_traefik_windows_control_center_container_port }}"
        passHostHeader: true
