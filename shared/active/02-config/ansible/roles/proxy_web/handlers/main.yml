---
# Handlers for proxy_web role
# Note: Container recreation is handled by `recreate: true` on the
# docker_container tasks, not by handlers. This avoids the issue where
# `restart: true` loses published port mappings.
