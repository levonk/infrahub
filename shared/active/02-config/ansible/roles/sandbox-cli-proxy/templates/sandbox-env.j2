# Generated by Ansible — do not edit
# Source: sandbox-cli-proxy role (deploy-sandbox-proxy.yml)
# Purpose: Resolved variable values for `just sandbox-*` recipes.
#          Each profile gets a set of SANDBOX_{PROFILE}_* env vars.
{% for profile in sandbox_cli_proxy_profiles %}
SANDBOX_{{ profile.name | upper }}_NETWORK={{ profile.network }}
SANDBOX_{{ profile.name | upper }}_PROXY_HOST=sandbox-iron-proxy-{{ profile.name }}
SANDBOX_{{ profile.name | upper }}_HTTP_PORT={{ profile.http_port }}
SANDBOX_{{ profile.name | upper }}_HTTPS_PORT={{ profile.https_port }}
SANDBOX_{{ profile.name | upper }}_CA_HOST_PATH={{ sandbox_cli_proxy_ca_dir }}/ca/ca.crt
{% endfor %}
SANDBOX_CA_CERT_CONTAINER_PATH=/etc/ssl/certs/sandbox-ca.crt
SANDBOX_CA_CERT_SYSTEM_PATH=/usr/local/share/ca-certificates/sandbox-ca.crt
