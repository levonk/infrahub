---
galaxy_info:
  role_name: vpn_netbird_control
  author: levonk
  description: Deploys the self-hosted NetBird control plane (management, signal, relay) as Docker containers.
  company: levonk
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions:
        - all
    - name: Ubuntu
      versions:
        - all
  galaxy_tags:
    - vpn
    - netbird
    - wireguard
    - networking
    - docker

dependencies:
  - role: docker-engine
