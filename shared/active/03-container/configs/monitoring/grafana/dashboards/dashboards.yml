# Home Lab In-a-Box - Grafana Dashboard Provisioning
# Purpose: Automatically load dashboards from the dashboards directory

apiVersion: 1  # project-lint: disable=magic-int  Grafana provisioning schema version, not a config value

providers:
  - name: 'Homelab Dashboards'
    orgId: 1
    folder: 'Homelab'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
