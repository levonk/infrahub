{
  "name": "",
  "version": "1.0.0",
  "description": "Egress firewall for untrusted workloads - NX orchestrated Docker service",
  "private": true,
  "scripts": {
    "build": "nx run :docker:build",
    "build:push": "nx run :docker:push",
    "up": "nx run :docker:up",
    "down": "nx run :docker:down",
    "lint": "just lint",
    "test": "echo 'Tests configured in project.json'",
    "affected:build": "nx affected -t docker:build",
    "graph": "nx graph"
  },
  "devDependencies": {
    "@nx-tools/nx-container": "^7.2.3",
    "@nx/js": "^22.7.2",
    "nx": "^22.7.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@*"
}
